dist: xenial
addons:
  apt:
    packages:
    - g++
    - cmake
    - libboost-all-dev
    - libssl-dev

language: c++
compiler: g++

install:
  - cmake -DCMAKE_BUILD_TYPE=Debug .
  - make
script:
  - ./kms-server & ./test_runner
